# ZaralynStudyMask（Zaralyn学习面具，后简写为Zara）

## 程序描述
Zara是一个NPatch模块，为软件刷入它之后可以为APP添加一层伪装层，它将修改软件的入口（主活动，Main Activity，后直接称为MainActivity）为一个特定的页面，只有在页面中按F10或者连击主页键5次才会进入软件原本的MainActivity（具体修改后MainActivity设计见下）

## 开发环境限制
- 当前处于termux环境，无法进行编译操作，请不要尝试
- 编译全部使用github actions
- Zara是一个NPatch模块，而不是一个完整的APK

## 界面设计

### 主界面（修改后的MainActivity）

主界面 是一个 Material Design 3 风格的高中英语学习界面，主要包含：

**顶部应用栏**：
- 高度：约 72dp
- 背景：天蓝色 (#1E88E5)
- 左侧图标：书本图标
- 标题：高中英语学习
- 右侧：无内容

**主页内容区**：
1. **年级选择区域**
   - 横向三个卡片：高一、高二、高三
   - 卡片高度：60dp
   - 默认选中状态：蓝色背景 + 白色边框
   - 未选中状态：浅灰背景 (#F3F3F3)
   - 左侧图标：学校帽图标
   - 文字：年级名称

2. **课本选择区域**
   - 横向两列布局
   - 课本卡片样式：
     - 圆角：16dp
     - 高度：120dp
     - 浅灰背景 (#F3F3F3)
     - 左上角：书本图标（天蓝色背景，白色图标）
     - 课本标题：加粗，黑色
     - 课本副标题：灰色
     - 底部：文件名（等宽字体，灰色边框）
   - hover 效果：轻微上移 + 阴影 + 浅蓝色渐变

**底部导航栏**：
- 高度：56dp
- 三个导航项：主页、课外、设置
- 选中状态：浅蓝色背景 (#D1E4FF) + 天蓝色图标
- 未选中状态：透明背景 + 灰色图标
- 图标：Material Symbols
- 图标名称：ic_home, ic_explore, ic_settings

**课外页面**：
- 四个卡片，2x2 网格布局
- 渐变背景（天蓝色到浅蓝色）
- 图标：耳机、麦克风、文档、编辑图标
- 标题和描述
- 提示正在开发

**设置页面**：
- 列表项：夜间模式、自动播放音频、隐私政策、用户协议、版本信息
- 每项包含标题、描述
- 开关控件：Material Design 风格的 Toggle Switch

**颜色系统**：
- 主色：#1E88E5 (天蓝色)
- 主色容器：#D1E4FF (浅蓝色)
- 表面色：#FFFFFF (白色)
- 表面容器：#F3F3F3 (浅灰)
- 描边色：#74777F (灰色)
- 文本色：#1D1B20 (深灰)
- 深色模式：#121212 (深色背景)

## 核心功能要求

### 1. MainActivity（主界面）
- **年级选择**：三个年级卡片，点击切换当前年级
- **课本显示**：根据选中的年级显示对应的课本列表
- **课本数据结构**：
  ```
  年级 10: 必修一、必修二、英语语法、词汇手册
  年级 11: 必修三、必修四、阅读训练、写作指南
  年级 12: 必修五、选修六、高考真题、冲刺复习
  ```
- **连击功能**：连击五次主页图标进入原MainActivity界面
- **底部导航**：切换主页、课外、设置三个页面

### 2. ReaderActivity（阅读界面）
- 显示 Markdown 内容
- 从 `/sdcard/EnglishBook/` 目录读取文件
- 如果文件不存在，从 assets 中读取 `test.md`
- 返回按钮

### 3. 原软件功能
- 保留软件的原有功能
- 通过连击五次主页图标进入软件原MainActivity
- 可以从学习界面无缝切换到原MainActivity

## 技术实现要点

### 必须使用的图标名称（Material Symbols）
- `ic_menu_book` - 书本图标（顶部应用栏）
- `ic_home` - 主页图标
- `ic_explore` - 探索图标（课外）
- `ic_settings` - 设置图标
- `ic_school` - 学校图标（年级选择）
- `ic_library_books` - 图书馆图标（课本选择）
- `icon_info` - 信息图标（命名规则提示）
（可从网络下载）

### Material Design 3 样式要求
- 所有圆角使用 16dp
- 卡片使用 `elevation` 属性
- 创建 `selector` drawable 实现状态变化
- 使用 MD3 颜色系统

### 页面切换动画
- 使用 FragmentTransaction
- 添加平滑的过渡动画
- 保存和恢复 Fragment 状态

### 文件读取
- 外部文件路径：`/sdcard/EnglishBook/`
- 内部测试文件：`assets/test.md`
- 需要处理文件权限

## 你可能犯的错误

### 错误 1：图标引用错误
- 在 `fragment_home.xml` 中引用了 `@drawable/ic_info`
- 但实际存在的图标是 `@drawable/icon_info`
- **教训**：创建图标时要明确命名，使用时要检查资源是否存在

### 错误 2：Book 类缺少 context 属性
- 在 `BookManager.getTestContent()` 中尝试访问 `book.context`
- 但 `Book` 类没有 `context` 属性
- **教训**：使用前要确认类的属性和方法，不要假设